# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Build output
app/dist/
server/dist/

# iOS can still be regenerated — no custom config there yet
app/ios/

# Android is tracked because it contains custom signing config in build.gradle.
# The keystore (*.jks) and local.properties are excluded via app/android/.gitignore.

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.suo
*.user

# Local binaries and scratch files
cloudflared.exe
docs_content.txt
